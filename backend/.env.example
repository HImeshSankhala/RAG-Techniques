# Copy to backend/.env and fill in. NEVER commit .env.

# Used from Phase 1 onward (core/llm.py).
ANTHROPIC_API_KEY=

# Origins allowed to call the API. Comma-separated.
CORS_ORIGINS=http://localhost:3000
